<h2>Nhập thông tin sinh viên</h2>

<form method="post">
    <input name="StudentCode" placeholder="Mã SV" />
    <input name="FullName" placeholder="Họ tên" />
    <button type="submit">Gửi</button>
</form>

@if (ViewBag.Result != null)
{
    <h3>@ViewBag.Result</h3>
}